#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    getchar();
    while(n--){

        stack <char> q;
        string a;
        getline(cin,a);
        if(a.length() == 0)
            cout<<"Yes"<<"\n";

        while(flag == 0){
            for(int i = 0;i < a.size();i++){
                if(q.top() == 0)
            }
        }

    }

    return 0;
}

